import Debug from 'debug'
const debug = Debug('unpacker:jest')

describe("First test suite for unpacker package", () => {
  test("First jest test case for unpacker", async () => {
    expect(1).not.toBe(2)
  })

})
